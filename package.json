{
    "engines": {
        "node": "17.x",
        "yarn": "1.22.x"
    },
    "scripts": {
        "heroku-prebuild": "cd server && yarn install --production=false",
        "postinstall": "cd server && yarn build",
        "start": "cd server && yarn start:compiled"
    }
}
